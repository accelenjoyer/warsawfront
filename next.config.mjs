/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ['www.the-warsaw.pl','warszawa-diaspora.pl','warsawhere.com'],
    },
};

export default nextConfig;
